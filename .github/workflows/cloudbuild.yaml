steps:
- name: 'curlimages/curl'
  entrypoint: 'sh'
  args:
    - -c
    - |
      chmod +x scripts/github/trigger_github_workflow.sh
      scripts/github/trigger_github_workflow.sh

options:
  logging: CLOUD_LOGGING_ONLY